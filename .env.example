# Frontend example environment
# Copy to frontend/.env and fill values.

# API base (adjust if backend is on a different port or ngrok)
VITE_API_URL=http://localhost:5000

# Enable AssemblyAI realtime backup for low-latency Listening
VITE_ENABLE_ASSEMBLYAI_BACKUP=1

# Optional: show AI retry toasts
VITE_SHOW_AI_RETRY_TOAST=0

# Optional: AudioWorklet -> PCM16 -> WebSocket streaming STT
# If enabled, the client streams 16kHz mono PCM frames to the backend gateway
# at /api/v1/stt/stream (proxied via Vite/nginx).
VITE_ENABLE_WS_PCM_STT=0
# Override only if you don't want the default (ws(s)://<host>/api/v1/stt/stream)
VITE_WS_PCM_STT_URL=
